name: Scratch
on:
  push:
  workflow_dispatch:
jobs:
  my-job:
    runs-on: windows-latest
    steps:
      - name: Update git for windows
        if: runner.os == 'Windows'
        run: sh -x /mingw64/bin/git-update-git-for-windows -y
        shell: pwsh
        env:
          GIT_TRACE: 1
